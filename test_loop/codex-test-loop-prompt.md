# Moved

The active prompt lives at:

`../scripts/codex-test-loop-prompt.md`

Run the loop with:

```bash
scripts/codex-test-loop.sh
```

The legacy wrapper `test_loop/codex-test-loop.sh` delegates to the active script.
