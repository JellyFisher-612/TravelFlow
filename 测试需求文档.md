# TravelFlow 测试需求文档

## 目标

建立一套可持续扩展的测试体系，让 Codex 可以在循环中逐步补齐测试、发现缺陷并修复缺陷。

测试关注用户体验，而不是只覆盖函数行数：

- 懂我：能正确识别用户意图，普通对话不误触发搜索。
- 少问：只在关键信息缺失时追问。
- 快回：问候、能力说明、简单元问题应直接回复。
- 稳推：外部检索失败时不编造事实，不生成不可靠计划。
- 能改：多轮对话中能修改出发地、时间、预算、偏好。
- 可信：车票、门票、开放时间、预约、天气、路线等硬约束要有来源或明确标注未核验。

## 核心链路

用户请求
→ 意图识别
→ 主智能体调度
→ Skill / 子 Agent 执行
→ 总结规划
→ 流式返回
→ 最终方案生成

## 测试优先级

### P0 基础测试设施

- 建立 `tests/` 目录。
- 提供可复用 fake LLM、fake memory、fake agent、fake MCP/搜索 fixture。
- 所有测试默认不访问真实网络和真实外部 MCP。
- 提供一条稳定命令运行测试，例如：`PYTHONPATH=agent_langchain pytest`。

验收标准：

- 新环境中运行测试不会因为缺少 API key 失败。
- 测试失败信息能定位到具体链路。

### P0 意图识别与直接回复

覆盖：

- 问候：`你好！`、`hello`、`在吗`
- 身份：`你是谁？`
- 能力：`你能做什么？`
- 感谢/确认：`谢谢`、`好的`
- 非旅行闲聊：不应默认触发 `search`
- LLM 连接失败时的规则兜底

验收标准：

- 普通对话返回 `agent_schedule=[]` 或最终 direct answer。
- 不出现搜索参考来源。
- 不加载 search agent。

### P0 规划类调度

覆盖：

- `帮我规划下周从上海去北京玩三天`
- 缺目的地、日期、天数、预算、节奏时的事项收集
- 规划请求按层级调度：`clarification -> search + memory -> plan`

验收标准：

- 关键字段缺失时不提前规划。
- 字段足够时 search/memory/plan 的上下文能串起来。

### P1 搜索智能体

覆盖：

- 天气问题走高德天气分支。
- 高铁/火车/余票/票价问题走 12306 分支。
- 行程规划场景先生成需求感知 `search_plan`。
- 亲子、小众、美食、雨天、长辈友好、摄影、预算等需求能影响检索任务。
- 门票、预约、开放时间、余票、票价、酒店库存等硬约束必须标记官方来源需求或未核验。

验收标准：

- `search_bundle.planning` 和 `quality` 字段可被 plan 使用。
- 外部查询失败时返回明确失败，不把未核验内容当事实。

### P1 编排与阻断

覆盖：

- clarification 缺关键字段时阻断 search/plan。
- search 失败时阻断 plan。
- memory 发现预算/节奏偏好缺失时按预期阻断或追问。
- plan 请求补充检索时，最多追加一轮 `search -> plan`。

验收标准：

- 不生成基于失败检索的完整行程。
- 追加检索结果能进入第二次 plan。

### P1 Web 流式响应

覆盖：

- `/api/chat` 普通响应。
- `/api/chat/stream` NDJSON 响应。
- direct answer 场景。
- 规划场景。
- 错误场景。

验收标准：

- trace 不重复。
- direct answer 不输出无关参考来源。
- stream 最终包含 `done`。

### P2 多轮修改

覆盖：

- 先说 `我要去北京玩两天`，再说 `我在苏州`。
- 修改日期、预算、节奏。
- 用户补充事项收集问题后继续规划。

验收标准：

- `rewritten_query` 合并历史上下文。
- 新字段覆盖旧字段。

### P2 最终方案质量

覆盖：

- 行程每天安排不要过密。
- 交通时间和地点顺序合理。
- 未核验的票务/预约/库存不写成已确认。
- 给出缺口说明和下一步建议。

验收标准：

- 最终文本可读、可信、没有明显编造硬约束。

## 回归用例池

Codex 循环可以逐步把这些用例转成自动化测试：

1. `你好！`
2. `你是谁？`
3. `你能做什么？`
4. `谢谢`
5. `杭州明天天气怎么样？`
6. `明天上海到杭州的高铁有哪些？`
7. `帮我规划下周从上海去北京玩三天`
8. `我想带孩子去杭州玩三天，想要小众、人少、好吃一点`
9. `故宫门票怎么预约？`
10. `我要去北京玩两天` → `我在苏州`

## 完成标记

当 P0、P1 主要链路都有自动化测试，且循环验证稳定通过时，可以在 `.codex-loop/progress.md` 中写入：

`ALL_TEST_REQUIREMENTS_DONE`
